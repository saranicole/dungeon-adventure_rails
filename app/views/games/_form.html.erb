<%= form_for @game do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.collection_select :player_id, Player.all, :id, :name, :prompt => true -%><br />
    <%= f.collection_select :dungeon_id, Dungeon.all, :id, :name, :prompt => true -%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>